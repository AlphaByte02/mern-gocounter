export interface ICounter {
    id: string;
    name: string;
    createdAt: string;
    updatedAt: string;
}

export interface IData {
    id: string;
    number: number;
    counterRef: string;
    createdAt: string;
    updatedAt: string;
}
